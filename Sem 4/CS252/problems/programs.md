1 what is a - e
```
Program hello.c
int a = 5; // Stored in [a]
section
int b[20]; // Stored in [b]

int main() { // Stored in [c]
	int x; // Stored in [d]
	int *p =(int*)
	malloc(sizeof(int)); // Stored in [e]
}
```
[[CS252-Slides-Sprin2026.pdf]] slide 15